name: Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
      id-token: write
      issues: write
      actions: read
    uses: DYB-Development/symphony/.github/workflows/gem-release.yml@main
